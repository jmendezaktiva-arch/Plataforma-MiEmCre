[build]
  publish = "public"
  functions = "netlify/functions"

[dev]
  command = "npm run build" # O el comando que uses para levantar tu front
  port = 8888
  publish = "public"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200